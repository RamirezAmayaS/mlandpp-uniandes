from numpy import random

# Mejor implementación de k-means. Retorna centroides correctos.
def kmeans(puntos,k):
    # Acá debería ir la lógica correcta de k-means
    centroides = random.randn(k,2)
    return centroides    
